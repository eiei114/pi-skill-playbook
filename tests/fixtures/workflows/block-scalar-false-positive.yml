name: Block scalar false positive fixture
on: workflow_dispatch
jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - name: Script mentions checkout syntax without using it
        run: |
          # documentation only
          uses: actions/checkout@not-a-real-reference
          echo "ok"
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
